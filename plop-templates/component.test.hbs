// src/components/{{capitalize name}}/{{capitalize name}}.test.tsx

// setupTests.tsx

// import react-testing methods
import { render, screen } from '@testing-library/react'

// the component to test
import { {{capitalize name}} } from './{{capitalize name}}'

test('renders {{capitalize name}} component', () => {
  //render is from @testing-library/react
  render(<{{capitalize name}} />)
  //expect assertion is from Jest
  // todo: replace this assertion...
  expect(screen.getByText(/{{capitalize name}}/)).toBeInTheDocument()
})
